{
  "name": "galia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:math": "turbo dev --filter=@galia/math",
    "build": "turbo build",
    "build:math": "turbo build --filter=@galia/math",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "packageManager": "pnpm@10.0.0"
}
